import type { IconProp, SizeProp } from '@fortawesome/fontawesome-svg-core'

export interface Props {
  color?: string
  size?: SizeProp
  icon: IconProp
  strokeWidth?: number
  strokeColor?: string
}
